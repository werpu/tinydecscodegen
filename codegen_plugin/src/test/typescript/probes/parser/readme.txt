some unit test probes patterns for the general parsing tests

(patterns and regressions which in time should cover the entire query area to 100%)
This 100% is a must because the query engine the central angle around everything
and might be isolated and reused for other tree structures in the long term
