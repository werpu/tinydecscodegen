import {Controller, Inject} from "TinyDecorations";import IScope = angular.IScope;

/**
 * Controller ${NAME}
 * @author ${AUTHOR}
 */
@Controller({
    name: "${NAME}",
    template: `${TEMPLATE}`,
    controllerAs: "${CONTROLLER_AS}"
})
export class ${NAME} {

    constructor(@Inject("$scope") private $scope: IScope, @Inject("$element") private $element: any) { //add your injections here
        //add your constructor code here
    }
    
}