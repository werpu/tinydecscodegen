import {Controller, Inject} from "TinyDecorations";
import * as angular from "angular";

/**
 * ComponentJson ${NAME}
 * @author ${AUTHOR}
 */
@Controller({
    name: "${NAME}",
    template: `${TEMPLATE}`,
    controllerAs: "${CONTROLLER_AS}"
})
export class ${NAME} {

    constructor(@Inject("$scope") private $scope: IScope, @Inject("$element") private $element: any) {
        //add your contstructor code here
    }
    
}